import a;

class Example {
    void bar(int k) {
        int b = foo(k,k);
        long c = b + c - 1;
        System . out . println(k / b, c);
    }


    int foo(Long privet, Integer medved) {
        return 10;
    }
}
